import { PagePlaceholder } from "@/components/page-placeholder";

export default function MapPage() {
  return (
    <PagePlaceholder
      title="Map"
      note="Open listings across the MP industrial corridor. Built in Block 5."
    />
  );
}
